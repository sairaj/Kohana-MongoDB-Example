<?php defined('SYSPATH') or die('No direct script access.');
return array(
'connection' => "mongodb://localhost:27017"
);

/*
$site_config = Kohana::config('site');
$site_config = Kohana::config('site.name');
$site_config = Kohana::config['site'];
*/